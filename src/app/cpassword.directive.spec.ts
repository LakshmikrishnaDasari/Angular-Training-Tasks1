import { CpasswordDirective } from './cpassword.directive';

describe('CpasswordDirective', () => {
  it('should create an instance', () => {
    const directive = new CpasswordDirective();
    expect(directive).toBeTruthy();
  });
});
