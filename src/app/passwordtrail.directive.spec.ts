import { PasswordtrailDirective } from './passwordtrail.directive';

describe('PasswordtrailDirective', () => {
  it('should create an instance', () => {
    const directive = new PasswordtrailDirective();
    expect(directive).toBeTruthy();
  });
});
